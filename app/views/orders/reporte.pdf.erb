<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <%= wicked_pdf_stylesheet_link_tag 'pdf' %>
    <link rel="stylesheets" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <meta charset="utf-8">
  </head>
  <body>
    <h1>Nota Fiscal Eletronica</h1>
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th><%= t('table.headers.product') %></th>
          &nbsp;
          <th><%= t('Valor Unid.') %></th>
          &nbsp;
          <th><%= t('Valor') %></th>
      </thead>
      <tbody>
        <% @orders.each do |orders| %>
        <% prod = Product.find(orders) %>
        <tr>
          <td><%= link_to prod.name, product_path(prod) %></td>
          &nbsp;
          <td><%= convert_number_to_money prod.value %></td>
        <td>    </td>
          &nbsp;
          <td><%= convert_number_to_money prod.value = prod.value + prod.value%></td>
        </tr>
      <% end %>
      </tbody>
    </table>
    <table>
      <thead>
        <tr>
          <th>Total:</th>
          <th> R$ 268,00
        </tr>
  </body>
</html>
